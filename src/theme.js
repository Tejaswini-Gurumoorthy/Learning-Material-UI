import {createTheme} from '@material-ui/core';

const primaryMainTheme= createTheme({
    palette:{
        background:{
            default: "#2148c0"
        }
    }
})

export default primaryMainTheme;